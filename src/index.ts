#!/usr/bin/env node
/**
 * CLI 入口点
 *
 * Mini Claude Code 的主入口文件。负责启动 CLI 应用，
 * 解析命令行参数并初始化 Agent 交互循环。
 *
 * 参考 Claude Code: src/cli.tsx
 * 简化：直接调用 CLI 模块，无 React/Ink 渲染层
 */

// TODO: Phase 1 — 导入 CLI 模块并启动 REPL
// import { parseArgs, runRepl } from './cli.js';
// import { Agent } from './agent.js';
// import { createModel, validateApiKey } from './provider.js';

console.log('Mini Claude Code — CLI Agent (skeleton)');
